package org.sackfix.field

import org.sackfix.common.validated.fields.SfFieldLocalMktDate
import java.time.LocalDate
import org.sackfix.common.validated.fields.SfFixDateFormats

/**
  * Generated by SackFix code generator on 20170521
  */
case class YieldCalcDateField(override val value: LocalDate) extends SfFieldLocalMktDate(701, value) {
  override def toString = appendStringBuilder().toString
  override def appendStringBuilder(b:StringBuilder = new StringBuilder()) = b.append("(701)YieldCalcDate=(").append(value).append(")")
}

object YieldCalcDateField {
  val TagId = 701  
  def apply(value: String) = try {
    new YieldCalcDateField(LocalDate.from(SfFixDateFormats.localMktDate.parse(value)))
  } catch {
    case ex: Exception => throw new IllegalArgumentException("new YieldCalcDate("+value.toString+") failed with exception", ex)
  } 
  def decode(a: Option[Any]) : Option[YieldCalcDateField] = a match {
    case Some(v) => decode(v)
    case _ =>  scala.Option.empty[YieldCalcDateField]
  }

  def decode(a: Any) : Option[YieldCalcDateField] = a match {
    case v: String => Some(YieldCalcDateField(v))
    case v: LocalDate => Some(YieldCalcDateField(v))
    case v: YieldCalcDateField => Some(v)
    case _ => scala.Option.empty[YieldCalcDateField]
  } 
}
