package org.sackfix.field

import org.sackfix.common.validated.fields.SfFieldLocalMktDate
import java.time.LocalDate
import org.sackfix.common.validated.fields.SfFixDateFormats

/**
  * Generated by SackFix code generator on 20170521
  */
case class ExpireDateField(override val value: LocalDate) extends SfFieldLocalMktDate(432, value) {
  override def toString = appendStringBuilder().toString
  override def appendStringBuilder(b:StringBuilder = new StringBuilder()) = b.append("(432)ExpireDate=(").append(value).append(")")
}

object ExpireDateField {
  val TagId = 432  
  def apply(value: String) = try {
    new ExpireDateField(LocalDate.from(SfFixDateFormats.localMktDate.parse(value)))
  } catch {
    case ex: Exception => throw new IllegalArgumentException("new ExpireDate("+value.toString+") failed with exception", ex)
  } 
  def decode(a: Option[Any]) : Option[ExpireDateField] = a match {
    case Some(v) => decode(v)
    case _ =>  scala.Option.empty[ExpireDateField]
  }

  def decode(a: Any) : Option[ExpireDateField] = a match {
    case v: String => Some(ExpireDateField(v))
    case v: LocalDate => Some(ExpireDateField(v))
    case v: ExpireDateField => Some(v)
    case _ => scala.Option.empty[ExpireDateField]
  } 
}
