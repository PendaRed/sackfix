package org.sackfix.field

import org.sackfix.common.validated.fields.SfFieldBoolean

/**
  * Generated by SackFix code generator on 20210314
  */
case class SolicitedFlagField(override val value: Boolean) extends SfFieldBoolean(377, value) {
  override def toString = appendStringBuilder().toString
  override def appendStringBuilder(b:StringBuilder = new StringBuilder()) = b.append("(377)SolicitedFlag=(").append({if (value) "Y" else "N"}).append(")")
}

object SolicitedFlagField {
  val TagId = 377  
  def apply(value: String) = try {
    new SolicitedFlagField(value match {
      case "Y" => true
      case "N" => false
      case _ => throw new IllegalArgumentException("SolicitedFlag.apply("+value+"] failed, bad value, expected Y or N.")
    })
  } catch {
    case ex: Exception => throw new IllegalArgumentException("new SolicitedFlag("+value.toString+") failed with exception", ex)
  } 
  def decode(a: Option[Any]) : Option[SolicitedFlagField] = a match {
    case Some(v) => decode(v)
    case _ =>  scala.Option.empty[SolicitedFlagField]
  }

  def decode(a: Any) : Option[SolicitedFlagField] = a match {
    case v: String => Some(SolicitedFlagField(v))
    case v: Boolean => Some(SolicitedFlagField(v))
    case v: SolicitedFlagField => Some(v)
    case _ => scala.Option.empty[SolicitedFlagField]
  } 
}
