package org.sackfix.field

import org.sackfix.common.validated.fields.SfFieldInt

/**
  * Generated by SackFix code generator on 20170404
  */
case class CrossPrioritizationField(override val value: Int) extends SfFieldInt(550, value) {
  override def toString = appendStringBuilder().toString
  override def appendStringBuilder(b:StringBuilder = new StringBuilder()) = b.append("(550)CrossPrioritization=(").append(value).append(")").append(CrossPrioritizationField.fixDescriptionByValue.getOrElse(value,""))
}

object CrossPrioritizationField {
  val TagId = 550 
  val None=0
  val BuySideIsPrioritized=1
  val SellSideIsPrioritized=2
  lazy val fixDescriptionByValue = Map(0->"NONE",1->"BUY_SIDE_IS_PRIORITIZED",
    2->"SELL_SIDE_IS_PRIORITIZED")
 
  def apply(value: String) = try {
    new CrossPrioritizationField(value.toInt)
  } catch {
    case ex: Exception => throw new IllegalArgumentException("new CrossPrioritization("+value.toString+") failed with exception", ex)
  } 
  def decode(a: Option[Any]) : Option[CrossPrioritizationField] = a match {
    case Some(v) => decode(v)
    case _ =>  scala.Option.empty[CrossPrioritizationField]
  }

  def decode(a: Any) : Option[CrossPrioritizationField] = a match {
    case v: String => Some(CrossPrioritizationField(v))
    case v: Int => Some(CrossPrioritizationField(v))
    case v: CrossPrioritizationField => Some(v)
    case _ => scala.Option.empty[CrossPrioritizationField]
  } 
}
