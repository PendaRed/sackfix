package org.sackfix.field

import org.sackfix.common.validated.fields.SfFieldLocalMktDate
import java.time.LocalDate
import org.sackfix.common.validated.fields.SfFixDateFormats

/**
  * Generated by SackFix code generator on 20170404
  */
case class EndDateField(override val value: LocalDate) extends SfFieldLocalMktDate(917, value) {
  override def toString = appendStringBuilder().toString
  override def appendStringBuilder(b:StringBuilder = new StringBuilder()) = b.append("(917)EndDate=(").append(value).append(")")
}

object EndDateField {
  val TagId = 917  
  def apply(value: String) = try {
    new EndDateField(LocalDate.from(SfFixDateFormats.localMktDate.parse(value)))
  } catch {
    case ex: Exception => throw new IllegalArgumentException("new EndDate("+value.toString+") failed with exception", ex)
  } 
  def decode(a: Option[Any]) : Option[EndDateField] = a match {
    case Some(v) => decode(v)
    case _ =>  scala.Option.empty[EndDateField]
  }

  def decode(a: Any) : Option[EndDateField] = a match {
    case v: String => Some(EndDateField(v))
    case v: LocalDate => Some(EndDateField(v))
    case v: EndDateField => Some(v)
    case _ => scala.Option.empty[EndDateField]
  } 
}
