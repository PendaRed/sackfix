package org.sackfix.field

import org.sackfix.common.validated.fields.SfFieldFloat

/**
  * Generated by SackFix code generator on 20170404
  */
case class AllocSharesField(override val value: Float) extends SfFieldFloat(80, value) {
  override def toString = appendStringBuilder().toString
  override def appendStringBuilder(b:StringBuilder = new StringBuilder()) = b.append("(80)AllocShares=(").append(value).append(")")
}

object AllocSharesField {
  val TagId = 80  
  def apply(value: String) = try {
    new AllocSharesField(value.toFloat)
  } catch {
    case ex: Exception => throw new IllegalArgumentException("new AllocShares("+value.toString+") failed with exception", ex)
  } 
  def decode(a: Option[Any]) : Option[AllocSharesField] = a match {
    case Some(v) => decode(v)
    case _ =>  scala.Option.empty[AllocSharesField]
  }

  def decode(a: Any) : Option[AllocSharesField] = a match {
    case v: String => Some(AllocSharesField(v))
    case v: Float => Some(AllocSharesField(v))
    case v: Double => Some(AllocSharesField(v.toFloat))
    case v: Int => Some(AllocSharesField(v.toFloat))
    case v: AllocSharesField => Some(v)
    case _ => scala.Option.empty[AllocSharesField]
  } 
}
