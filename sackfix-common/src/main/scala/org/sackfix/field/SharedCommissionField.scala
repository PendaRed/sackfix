package org.sackfix.field

import org.sackfix.common.validated.fields.SfFieldFloat

/**
  * Generated by SackFix code generator on 20170404
  */
case class SharedCommissionField(override val value: Float) extends SfFieldFloat(858, value) {
  override def toString = appendStringBuilder().toString
  override def appendStringBuilder(b:StringBuilder = new StringBuilder()) = b.append("(858)SharedCommission=(").append(value).append(")")
}

object SharedCommissionField {
  val TagId = 858  
  def apply(value: String) = try {
    new SharedCommissionField(value.toFloat)
  } catch {
    case ex: Exception => throw new IllegalArgumentException("new SharedCommission("+value.toString+") failed with exception", ex)
  } 
  def decode(a: Option[Any]) : Option[SharedCommissionField] = a match {
    case Some(v) => decode(v)
    case _ =>  scala.Option.empty[SharedCommissionField]
  }

  def decode(a: Any) : Option[SharedCommissionField] = a match {
    case v: String => Some(SharedCommissionField(v))
    case v: Float => Some(SharedCommissionField(v))
    case v: Double => Some(SharedCommissionField(v.toFloat))
    case v: Int => Some(SharedCommissionField(v.toFloat))
    case v: SharedCommissionField => Some(v)
    case _ => scala.Option.empty[SharedCommissionField]
  } 
}
